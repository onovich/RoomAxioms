# UI / 玩法原型

直接打开 `index.html` 即可运行；无网络依赖。

建议路径：

1. 根据三只酒瓶调查 B2；
2. 调查 C1，推出并标记 D1；
3. 调查 A3、C3；
4. 推出并标记 B4；
5. 提交结论。

交互：

- 左键按当前工具操作；
- 右键未知格循环“访客 → 安全 → 无标记”；
- 顶部“验证层”显示候选布局、强制格和目标剧透；
- 点击规则卡查看其实际棋盘内作用范围；
- “邻域定义”展示边缘语义。

该原型内置针对基准样例的候选危险布局枚举，用于验证 UI，不是生产求解器实现。
