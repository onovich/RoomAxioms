# Phase 26 Candidate Review Log

Status: Round 3 ledger scaffold.
Guide: `docs/phase-26-workbench-guided-puzzle-ladder-production-goal-mode-execution-guide.md`

This log is the source of truth for Phase 26 candidate attempt accounting. A
candidate does not count as one of the required 12 serious attempts until it has
an entry here with at least report and score evidence.

## Candidate Evidence Contract

Each serious candidate must record:

- candidate id;
- file path under `content/experimental/phase-26/` or repaired shipped case path;
- intended proof skeleton;
- authoring `report` command and result;
- authoring `score` command and result;
- workbench diagnostics summary or exported draft note;
- anti-clone / degeneracy result before promotion;
- novelty claim status;
- copy review status;
- decision: rejected, deferred, repaired, promoted, or quarantined;
- exact reason for rejection or promotion.

Workflow details live in `docs/phase-26/authoring-workflow.md`.

## Skeleton Catalog

| Skeleton | Template | Intended use |
| --- | --- | --- |
| P26-S1 Crossing Overlap Unlock | `content/experimental/phase-26/templates/p26-s1-crossing-overlap-unlock-template.json` | Non-singleton overlap that becomes useful after a classic deduction. |
| P26-S2 Comparative Balance Fork | `content/experimental/phase-26/templates/p26-s2-comparative-balance-fork-template.json` | Comparison where a later observation breaks the balance. |
| P26-S3 Conditional Frontier Unlock | `content/experimental/phase-26/templates/p26-s3-conditional-frontier-unlock-template.json` | Conditional rule activated after an earlier human deduction. |
| P26-S4 Difference Intersection Braid | `content/experimental/phase-26/templates/p26-s4-difference-intersection-braid-template.json` | Local difference/intersection chain that avoids prior answer-pattern clones. |
| P26-S5 Object Gated Local Saturation | `content/experimental/phase-26/templates/p26-s5-object-gated-local-saturation-template.json` | Object identification unlocks later neighborhood/ring saturation. |
| P26-S6 Two Wave Reveal Frontier | `content/experimental/phase-26/templates/p26-s6-two-wave-reveal-frontier-template.json` | First proof wave changes the material scope for the second wave. |
| P26-S7 Grammar And Classic Hybrid | `content/experimental/phase-26/templates/p26-s7-grammar-classic-hybrid-template.json` | Phase 24 grammar is necessary but supported by classic readable counts. |

## Attempt Matrix

| # | Candidate | Path | Skeleton | Report | Score | Workbench / diagnostics | Anti-clone / degeneracy | Novelty | Copy | Decision | Reason |
| ---: | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `p26-c01-overlap-unlock` | `content/experimental/phase-26/candidates/p26-c01-overlap-unlock.json` | P26-S1 | PASS: ok true; initial guest layouts 9; final guests D1,B4; proof 1 wave / 13 deductions; no truncation. | PASS: 22.16 / band 5 uncalibrated. | CLI diagnostics PASS mechanically, but target-4 review misses `proof-wave-count` and `frontier-unlock-count`; proof techniques do not include any `SCOPE_OVERLAP_*` technique. | FAIL: full selector anti-clone reports exact proof-trace match for `case-004,p26-c01-overlap-unlock`; no C01-specific degeneracy hard-fail observed, but baseline selector still contains known inherited failures. | Not promotion-ready; no novelty claim added because clone gate fails. | PASS for experimental copy: R7 names the seven cells explicitly and does not rely on a hidden region label. | rejected | Serious candidate rejected. It is schema/solver/proof valid, but the scope-overlap rule does not materially enter the human proof (`minimize --require-technique SCOPE_OVERLAP_COUNT_SATURATED` reports `TECHNIQUE_RETENTION_FAILED`) and anti-clone finds an exact proof-trace clone of `case-004`. |
| 2 | `p26-c02-comparative-balance` | `content/experimental/phase-26/candidates/p26-c02-comparative-balance.json` | P26-S2 | FAIL: schema and target rules pass; initial guest layouts 2; proof noGuess false; final guest layout not unique; 1 wave / 4 deductions; no truncation. | PASS: 16.21 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; techniques include `COMPARATIVE_COUNT_SATURATED` and `LOCAL_COUNT_SATURATED`; minimize retains `COMPARATIVE_COUNT_SATURATED` but still fails proof/final uniqueness. | FAIL: degeneracy hard-fail `comparative-left:R3:singleton-effective-scope`; rule-family diversity reviewer-blocking `redundant-rules`; no promotion anti-clone pass. | Not promotion-ready; no novelty claim added because proof, degeneracy, and redundancy gates fail. | PASS for experimental copy: R3 explicitly names A1/B1/A2 and C1/D1/C2; no hidden region semantics. | rejected | Serious candidate rejected. It proves Phase 24 comparative proof support can fire and survive minimization, but the candidate remains a one-wave proof gap with final non-unique guests, a singleton comparative side, and redundant R1/R4/R6 rules. |
| 3 | `p26-c03-conditional-frontier` | `content/experimental/phase-26/candidates/p26-c03-conditional-frontier.json` | P26-S3 | FAIL: schema and target rules pass; initial guest layouts 2; proof noGuess false; final guest layout not unique; 1 wave / 2 deductions; no truncation. | PASS: 8.95 / band 2 uncalibrated. | CLI diagnostics `repair-proof`; delayed-trigger version only uses `LOCAL_COUNT_SATURATED`, not `CONDITIONAL_COUNT_*`; public-condition repair was tried but collapsed to opening unique / 0 proof waves. | FAIL: reviewer-blocking `line:R4:near-count-giveaway`; reviewer-blocking `redundant-rules`; baseline selector still has inherited hard failures. | Not promotion-ready; no novelty claim added because conditional technique retention, proof, and quality gates fail. | PASS for experimental copy: R3 and R4 explicitly name their cells; no hidden scope label. | rejected | Serious candidate rejected. It shows the intended delayed condition cannot currently use safe-cell object reveals to activate `conditionalCount`, while the public-condition fallback becomes a zero-wave opening-unique fixture instead of a puzzle. |
| 4 | `p26-c04-difference-braid` | `content/experimental/phase-26/candidates/p26-c04-difference-braid.json` | P26-S4 | FAIL: schema and target rules pass; initial guest layouts 4; proof noGuess false; final guest layout not unique; `EXPLANATION_GAP` on C4; 1 wave / 7 deductions; no truncation. | PASS: 15.10 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; techniques include `LOCAL_COUNT_SATURATED` and `LOCAL_SCOPE_DIFFERENCE`; target-4 review misses `proof-wave-count`, `deduction-count`, `material-rule-family-count`, and `frontier-unlock-count`. | FAIL for promotion: C04 degeneracy passes and material families pass, but selector anti-clone command remains fail because C04 lacks a novelty claim; broader pack also reports inherited baseline issues. | Not promotion-ready; no novelty claim added because proof/final uniqueness and intended intersection retention fail. | PASS for experimental copy: rules use object/neighborhood wording, no named hidden region, and no highlight-only scope dependency. | rejected | Serious candidate rejected. The D4 public-bottle repair reduced opening layouts to 4 and retained `LOCAL_SCOPE_DIFFERENCE`, but the proof still cannot explain C4, final guests remain non-unique, `LOCAL_SCOPE_INTERSECTION` never appears, and the case collapses to a one-wave/tutorial-or-baseline review instead of the intended difference/intersection braid. |
| 5 | `p26-c05-object-gated` | `content/experimental/phase-26/candidates/p26-c05-object-gated.json` | P26-S5 | FAIL: schema and target rules pass; initial guest layouts 10 with solver truncation; proof noGuess false; final guest layout not unique; 1 wave / 2 deductions; proof gaps on A3, C3, A4, B4, C4. | PASS: 18.81 / band 5 uncalibrated, but score metrics report solver truncation. | CLI diagnostics `raise-caps-or-simplify`; techniques include `LOCAL_COUNT_ALL_REMAINING` and `KNOWN_SAFE_FROM_NON_GUEST_OBJECT`; minimize retains both required object-gated techniques. | Not promotion-run to completion: degeneracy passes in report, but full selector anti-clone exceeded a reasonable local runtime and was stopped after C05 was already blocked by truncation, proof gaps, and final non-uniqueness. | Not promotion-ready; no novelty claim added because correctness and cap gates fail. | PASS for experimental copy: object names are plain Chinese (`酒瓶`, `垃圾桶`) and no internal anchor wording or hidden named region is exposed. | rejected | Serious candidate rejected. It proves the intended object-gated skeleton can fire: R1 identifies B3 as a garbage bin and the safe-from-object deduction is retained. The draft lacks enough independent closing pressure, so R2's expected downstream no-guest saturation becomes solver-only proof gaps and the guest layout remains non-unique. |
| 6 | `p26-c06-two-wave-frontier` | `content/experimental/phase-26/candidates/p26-c06-two-wave-frontier.json` | P26-S6 | FAIL: schema and target rules pass; initial guest layouts 14; proof noGuess false; final guest layout not unique; 2 waves / 4 deductions; no truncation; proof gaps on D2, A3, B3, C3. | PASS: 17.52 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; target-4 review records `frontierUnlockCount = 1`, 4 material rule families, and retained `REGION_COUNT_SATURATED` + `LOCAL_COUNT_ALL_REMAINING` after minimization. | FAIL for promotion: C06-specific degeneracy passes for R3 and R5 and rule-family diversity passes, but selector anti-clone remains fail because no novelty claim exists and the comparison set still contains inherited baseline hard failures. | Not promotion-ready; no novelty claim added because proof/final uniqueness gates fail. | PASS for experimental copy: R3 and R5 explicitly name their cells (`A1、B1、C1`; `B4、C4、D4`) and do not rely on hidden region membership. | rejected | Serious candidate rejected but kept as useful near-miss evidence. It forms the intended two-wave frontier: R3 reveals A1/B1/C1, B1 becomes a bottle, and R4 then confirms B2 as a guest. The bottom-line frontier still leaves non-unique layouts and solver-only safe cells, so it needs a second readable closing rule rather than promotion. |
| 7 | `p26-c07-grammar-classic` | `content/experimental/phase-26/candidates/p26-c07-grammar-classic.json` | P26-S7 | FAIL: schema and target rules pass; initial guest layouts 12 but hit the default node cap; proof noGuess false; final guest layout not unique; `GUESS_POINT`; 2 waves / 4 deductions. | PASS with caveat: 20.47 / band 5 uncalibrated, but score metrics report solver truncation. | CLI diagnostics `raise-caps-or-simplify`; techniques include `COMPARATIVE_COUNT_SATURATED` and `LOCAL_COUNT_SATURATED`; minimize retains `COMPARATIVE_COUNT_SATURATED`; target-4 review misses proof-wave-count, deduction-count, frontier-unlock-count, and shared-variable-overlap-count. | FAIL for promotion: C07-specific degeneracy passes, rule-family diversity passes with 4 material families and no redundant rules, but selector anti-clone remains fail because no novelty claim exists and the comparison set still contains inherited baseline issues. | Not promotion-ready; no novelty claim added because cap, no-guess, and final uniqueness gates fail. | PASS for experimental copy: R3, R5, and R6 use explicit coordinate/object wording and do not rely on hidden region membership. | rejected | Serious candidate rejected. It proves a Phase 24 grammar/classic hybrid can trigger and retain `COMPARATIVE_COUNT_SATURATED` while classic bin-local pressure also participates, but the proof stops at a second-wave guess point and the remaining guest layout is not unique. |
| 8 | `p26-c08-overlap-repair` | `content/experimental/phase-26/candidates/p26-c08-overlap-repair.json` | P26-S1 | FAIL: schema and target rules pass; initial guest layouts 6; no truncation; proof noGuess false; final guest layout not unique; 1 wave / 1 deduction; 5 `EXPLANATION_GAP` issues. | PASS with caveat: 12.95 / band 3 uncalibrated. | CLI diagnostics `repair-proof`; techniques include `SCOPE_OVERLAP_COUNT_ALL_REMAINING`; minimize retains `SCOPE_OVERLAP_COUNT_ALL_REMAINING`; 4 material rule families and no redundant rules. Earlier drafts showed derived safe/guest deductions do not feed later `scopeOverlapCount` count summaries as exact observations. | FAIL: C08-specific hard degeneracy `scope-overlap:R2:singleton-effective-scope:direct-count-giveaway`; reviewer-blocking `region:R4:near-count-giveaway`; selector anti-clone also fails because no novelty claim exists and inherited baseline issues remain. | Not promotion-ready; no novelty claim added because proof/final uniqueness and hard degeneracy gates fail. | PASS for experimental copy: overlap and bottom scopes use explicit coordinate lists and no hidden region names. | rejected | Serious candidate rejected. It repairs C01 only far enough to retain the scope-overlap proof technique; the only retained form is a direct initial-observation giveaway, while attempts to make a classic deduction unlock the overlap failed because proof count summaries require exact observations rather than derived safe/guest deductions. |
| 9 | `p26-c09-comparative-repair` | `content/experimental/phase-26/candidates/p26-c09-comparative-repair.json` | P26-S2 | FAIL: schema and target rules pass; initial guest layouts 6; no truncation; proof noGuess false; final guest layout not unique; 1 wave / 2 deductions; proof gaps on A3 and B3. | PASS with caveat: 14.06 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; techniques include `COMPARATIVE_COUNT_ALL_REMAINING`; minimize retains `COMPARATIVE_COUNT_ALL_REMAINING`; 3 material rule families and no redundant rules. | FAIL for promotion: C09-specific comparative degeneracy checks pass and rule-family diversity passes, but R3 has reviewer-blocking `near-count-giveaway`; selector anti-clone also fails because no novelty claim exists and inherited baseline issues remain. | Not promotion-ready; no novelty claim added because proof/final uniqueness and reviewer-blocking degeneracy gates fail. | PASS for experimental copy: comparative and region rules use explicit coordinate lists with no hidden scope naming. | rejected | Serious candidate rejected but useful. It repairs the C02 singleton flaw: comparative-left has 2 effective unknowns and `COMPARATIVE_COUNT_ALL_REMAINING` forces A1/B1 together. The late layout still needs a readable closing rule for A3/B3 and the east-lane region is too close to a direct giveaway. |
| 10 | `p26-c10-frontier-repair` | `content/experimental/phase-26/candidates/p26-c10-frontier-repair.json` | P26-S6 | FAIL: schema and target rules pass; initial guest layouts 9; no truncation; proof noGuess false; final guest layout not unique; 2 waves / 4 deductions; proof gaps on D2, A3, and B3. | PASS with caveat: 16.41 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; target-4 review records `frontierUnlockCount = 1`; techniques include `REGION_COUNT_SATURATED` and `LOCAL_COUNT_ALL_REMAINING`; minimize retains `LOCAL_COUNT_ALL_REMAINING`; redundant rule count fixed to 0 after removing the decorative bottle global. | FAIL for promotion: C10-specific degeneracy passes and rule-family diversity passes with 3 material families; selector anti-clone still fails because no novelty claim exists and inherited baseline issues remain. | Not promotion-ready; no novelty claim added because no-guess/final uniqueness gates fail. | PASS for experimental copy: R3 and R6 explicitly name their cells and do not rely on hidden region membership. | rejected | Serious candidate rejected but useful near-miss. It restores the S6 two-wave shape after one repair draft regressed to a one-wave proof: R3 reveals B1 as bottle, then R4 confirms B2 as guest. Adding the guest-neighborhood rule plus public C3 still cannot make D2/A3/B3 human-explainable, so late closure remains the blocker. |
| 11 | `p26-c11-replacement-attempt` | `content/experimental/phase-26/candidates/p26-c11-replacement-attempt.json` | P26-S5 | PASS mechanically but not promotion-ready: schema/target/initial satisfiable pass; initial guest layouts 1; no truncation; noGuess/final uniqueness true only because opening guest layout is already unique; 0 waves / 0 deductions. | PASS with caveat: 5.93 / band 1 uncalibrated. | CLI diagnostics `ready-for-experimental-review` mechanically, but difficulty review classifies tutorial-or-baseline and misses opening ambiguity, proof-wave-count, deduction-count, frontier-unlock-count, shared-variable-overlap-count, redundant-rule-count, and degeneracy-status; minimize loses required object-gated techniques because there is no proof wave. | FAIL for promotion: lightweight anti-clone against `case-015` reports C11 reviewer-blocking `line:R4:near-count-giveaway`, `line:R5:near-count-giveaway`, `region:R6:near-count-giveaway`, and redundant R6. Full selector anti-clone was stopped after the candidate was already blocked by opening uniqueness. | Not promotion-ready; no novelty claim added because opening-unique/zero-wave and degeneracy gates fail. | PASS for experimental copy: all scopes use object terms or explicit coordinate lists; no hidden region label or internal anchor wording. | rejected | Serious candidate rejected. It repaired C05's target contradiction and cap problem, and it avoids case-015's hard singleton failures, but the resulting object-gated replacement is solved by the opening observations before any human proof step. This is useful evidence that adding enough public line/region pressure to close the object-gated layout can collapse the puzzle into a zero-wave fixture. |
| 12 | `p26-c12-final-synthesis` | `content/experimental/phase-26/candidates/p26-c12-final-synthesis.json` | P26-S7 | FAIL: schema/target/initial satisfiable pass; initial guest layouts 12; no truncation; proof noGuess false; final guest layout not unique; `GUESS_POINT`; 2 waves / 2 deductions. | PASS with caveat: 15.27 / band 4 uncalibrated. | CLI diagnostics `repair-proof`; `COMPARATIVE_COUNT_ALL_REMAINING` fires and is retained by minimize; target-4 review still misses deduction-count, frontier-unlock-count, shared-variable-overlap-count, and redundant-rule-count. | FAIL for promotion: lightweight anti-clone against C07/C09 reports exact proof-trace clone between C09 and C12; C12 degeneracy itself passes, but rule-family diversity flags R3/R4 as redundant. | Not promotion-ready; no novelty claim added because proof/final uniqueness, exact C09 proof-trace clone, and redundant-rule gates fail. | PASS for experimental copy: comparative and bottom scopes use explicit coordinate lists; object rules use plain `酒瓶`/`垃圾桶` wording. | rejected | Serious candidate rejected. It synthesizes the best C09 comparative opener with object-local pressure and avoids C09's near-count degeneracy warning, but the object rules do not enter the material proof. The result keeps only the same two comparative deductions as C09, then stops at a guess point with final non-uniqueness. |

| 13 | `p26-c13-frontier-closure` | `content/experimental/phase-26/candidates/p26-c13-frontier-closure.json` | P26-S6 repair | FAIL: schema/target/initial satisfiable pass; initial guest layouts 3; no truncation; proof noGuess false; final guest layout not unique; `GUESS_POINT`; 2 waves / 4 deductions. | PASS with caveat: 13.50 / band 3 uncalibrated. | CLI diagnostics `repair-proof`; final variant retains `LOCAL_COUNT_ALL_REMAINING` and `REGION_COUNT_SATURATED` under minimize, but earlier variants showed derived safe cells from region counts do not feed the bottle-local count, so A1/B1/C1 had to become opening observations before B2 could be human-explained. | FAIL for promotion: lightweight anti-clone against C06/C10/C13 reports C13 hard degeneracy `region:R2:singleton-effective-scope` because A1/B1/C1 are all public, plus reviewer-blocking redundant rules. R1/R2/R4/R5 are redundant in the authoring report. | Not promotion-ready; no novelty claim added because no-guess/final uniqueness, degeneracy, and redundancy gates fail. | PASS for experimental copy: scopes use explicit coordinate lists and no hidden region names; R6 is still a direct three-cell no-guest closure and should not be counted as hard evidence. | rejected | Round 18 C10 repair attempt rejected. It closes D2/A3/B3 only by adding a direct three-cell no-guest region, and it explains B2 only after A1/B1/C1 become public observations. The two intended techniques are retained, but the case collapses into a weak degenerate proof and still does not reach final uniqueness. |
| 14 | `p26-c14-comparative-object-bridge` | `content/experimental/phase-26/candidates/p26-c14-comparative-object-bridge.json` | P26-S7 repair | PASS mechanically but not promotion-ready: schema/target/initial satisfiable pass; initial guest layouts 1; no truncation; noGuess/final uniqueness true only because the opening guest layout is already unique; 0 waves / 0 deductions. | PASS with caveat: 5.60 / band 1 uncalibrated. | CLI diagnostics `ready-for-experimental-review` mechanically. First draft without public B1 produced explanation gaps on A3/B3/B4 after the comparative opener; guest reveal repair was schema-invalid; final legal repair uses public B1 mirror, but collapses all intended techniques out of the proof. Minimize loses `COMPARATIVE_COUNT_ALL_REMAINING`, `LOCAL_COUNT_ALL_REMAINING`, and `LOCAL_COUNT_SATURATED` because there is no proof wave. | FAIL for promotion: anti-clone/degeneracy against C09/C12/C14 reports C14 hard degeneracy `comparative-left:R2:singleton-effective-scope`; material families pass and no rules are redundant, but opening uniqueness, zero proof, and singleton comparative side are hard blockers. | Not promotion-ready; no novelty claim added because opening-unique/zero-wave and degeneracy gates fail. | PASS for experimental copy: all scopes use explicit coordinate lists or plain object terms (`镜子`, `酒瓶`, `垃圾桶`) with no hidden region labels. | rejected | Round 19 C12 repair attempt rejected. It proves that object-bridge pressure can close the C09/C12 comparative line only by over-constraining the opening: making B1 a public non-guest lets the object rules become material, but it reduces the comparative left side to one effective unknown and removes the player-facing deduction chain. |
| 15 | `p26-c15-overlap-chain-repair` | `content/experimental/phase-26/candidates/p26-c15-overlap-chain-repair.json` | P26-S1 repair | FAIL: schema/target/initial satisfiable pass; initial guest layouts 4; no truncation; proof noGuess false; final guest layout not unique; explanation gaps on B1 and A2; 1 wave / 4 deductions. | PASS with caveat: 13.69 / band 3 uncalibrated. | CLI diagnostics `repair-proof`; techniques are only `REGION_COUNT_ALL_REMAINING` and `REGION_COUNT_SATURATED`. Minimize with required `SCOPE_OVERLAP_COUNT_SATURATED`, `LOCAL_COUNT_SATURATED`, and `LOCAL_COUNT_ALL_REMAINING` reports all required techniques missing. This confirms the intended derived-A1-guest -> overlap-saturation bridge does not enter the human proof. | FAIL for promotion: anti-clone/degeneracy against C01/C08/C15 reports C15 hard degeneracy `region:R2:singleton-effective-scope:direct-count-giveaway` and reviewer-blocking redundant R4. C15's scope-overlap rule itself passes degeneracy and is material, but it still produces no `SCOPE_OVERLAP_*` proof technique. | Not promotion-ready; no novelty claim added because proof/final uniqueness, missing overlap technique, singleton entry rule, and redundancy gates fail. | PASS for experimental copy: overlap and region scopes are explicit coordinate lists; no hidden highlight semantics. | rejected | Round 20 C08 repair attempt rejected. It preserves opening ambiguity and gives the overlap rule a non-degenerate scope, but the human proof stops before using derived A1 as the known overlap guest. Solver can see B1/A2 are safe; the proof system reports them as explanation gaps rather than `SCOPE_OVERLAP_COUNT_SATURATED` deductions. |

## Promotion Accounting

| Case | Source candidate | Promoted path | PASS gate evidence | Notes |
| --- | --- | --- | --- | --- |
| None after Round 20 audit | 15 attempted candidates | None | `docs/phase-26/promotion-gate-audit.md` | No Phase 26 candidate currently passes the full promotion gate; selector remains unchanged. |
| None after Round 21 ladder/copy review | 15 attempted candidates | None | `docs/phase-26/ladder-copy-review.md` | Current 10-case selector retained with caveats; no rejected or experimental Phase 26 content promoted. |

## Rejection Themes

Use this section to summarize repeated failures as evidence accumulates.

- Singleton or direct-count giveaway: `p26-c02-comparative-balance` fails degeneracy on `comparative-left:R3:singleton-effective-scope`; this is a clearer comparative-specific trap than the old region/line singleton failures.
- Singleton or direct-count giveaway: `p26-c03-conditional-frontier` avoids a hard singleton failure but still hits reviewer-blocking `line:R4:near-count-giveaway`, so the conditional consequence leaves too little real frontier.
- One-family closure: none yet.
- Clone-like proof trace or shrink signature: `p26-c01-overlap-unlock` is mechanically valid but anti-clone reports an exact proof-trace match with `case-004`, so adding a `scopeOverlapCount` shell did not create a distinct proof shape.
- Highlight-dependent or internal copy: none yet.
- Phase 24 grammar support/copy blocker: `p26-c01-overlap-unlock` has a `scopeOverlapCount` rule family, but proof techniques do not include `SCOPE_OVERLAP_*` and `minimize --require-technique SCOPE_OVERLAP_COUNT_SATURATED` reports `TECHNIQUE_RETENTION_FAILED`.
- Phase 24 grammar support/copy blocker: `p26-c02-comparative-balance` does retain `COMPARATIVE_COUNT_SATURATED`, but current candidate design still fails no-guess/final uniqueness and degeneracy; comparative proof support exists, but authoring must avoid fully fixed/singleton comparison sides and decorative follow-on rules.
- Phase 24 grammar support/copy blocker: `p26-c03-conditional-frontier` shows a proof-support boundary for delayed `conditionalCount`: safe-cell deductions do not reveal object facts early enough to activate the condition. Making the condition public at opening collapses into `initialGuestLayouts = 1` and `proofWaveCount = 0`, repeating the Phase 24 fixture problem.
- Phase 24 grammar support/copy blocker: `p26-c04-difference-braid` retains `LOCAL_SCOPE_DIFFERENCE` after minimization, but `LOCAL_SCOPE_INTERSECTION` never appears and the remaining C4 guest requires solver-only reasoning. The difference/intersection braid skeleton needs a second real local overlap or a different target, not another public reveal.
- Proof-gap failure: `p26-c04-difference-braid` shows that a candidate can pass degeneracy and material-family checks while still being promotion-blocked by no-guess/final-uniqueness failure; C4 is solver-proven but not human-explained.
- Object-gated support exists but needs closing pressure: `p26-c05-object-gated` retains `LOCAL_COUNT_ALL_REMAINING` and `KNOWN_SAFE_FROM_NON_GUEST_OBJECT`, confirming the proof system can derive a hidden garbage bin from a bottle count. The follow-on no-guest cells around that derived object are not human-explained, and the candidate is also blocked by solver truncation and final non-uniqueness.
- Cap/performance warning: `p26-c05-object-gated` shows that an apparently small 4x4 object-gate draft can still hit the default `initialGuestLayouts` node cap when the remaining object space is too unconstrained. Future S5 repairs should add readable non-guest/object constraints before broadening the board.
- Two-wave near miss: `p26-c06-two-wave-frontier` proves the S6 skeleton can produce a real two-wave chain without singleton degeneracy: the first region wave reveals a bottle, and the second local-count wave confirms B2 as guest. It still fails promotion because the remaining bottom-line branch is not closed by human-readable deductions.
- Closing-rule need: `p26-c06-two-wave-frontier` suggests future S6 repairs should add a non-singleton second frontier that explains the safe cells D2/A3/B3/C3 or disambiguates B4/C4/D4, instead of relying on global count plus a single bottom-line count.
- Two-wave repair progress: `p26-c10-frontier-repair` removes the redundant bottle-global rule, passes C10-specific degeneracy and rule-family gates, and keeps the two-wave `REGION_COUNT_SATURATED` -> `LOCAL_COUNT_ALL_REMAINING` shape with `frontierUnlockCount = 1`. It still fails no-guess/final uniqueness because D2/A3/B3 remain solver-only safe cells.
- Guest-neighborhood closure limit: `p26-c10-frontier-repair` tested whether a derived B2 guest could activate a no-adjacent-guest rule to explain A3/B3. The current proof still does not emit those safe deductions after B2, so future S6 repairs should not rely on derived guest subjects for immediate closure unless proof semantics are deliberately expanded later.
- Grammar/classic hybrid support exists but still needs closure: `p26-c07-grammar-classic` retains `COMPARATIVE_COUNT_SATURATED` after minimization and removes the earlier comparative redundancy problem, with C07-specific degeneracy and rule-family checks passing. It still fails promotion because initial layout enumeration truncates, no-guess/final uniqueness fail, and the second proof wave has no human-readable closing deduction.
- Comparative support now has two distinct failure modes: `p26-c02-comparative-balance` is blocked by singleton/redundant design, while `p26-c07-grammar-classic` avoids those design flaws but still lacks enough closing pressure. Future comparative repairs should keep C07's explicit non-redundant scope style and add a readable late disambiguator.
- Scope-overlap retention currently needs exact opening observations: `p26-c08-overlap-repair` tried safe-cell and guest-deduction unlocks before settling on a direct opening-observation overlap. The retained technique proves the grammar path works, but the accepted proof does not chain derived safe/guest deductions into later `scopeOverlapCount` summaries, so the only retained draft becomes a singleton direct-count giveaway.
- Scope-overlap promotion blocker: `p26-c08-overlap-repair` has 4 material families and retained `SCOPE_OVERLAP_COUNT_ALL_REMAINING`, but anti-clone/degeneracy reports `scope-overlap:R2:singleton-effective-scope:direct-count-giveaway` and R4 near-count giveaway. Future S1 work needs either proof support for derived observations feeding count summaries or a scope design where exact opening observations leave at least two effective unknown overlap cells.
- Comparative repair progress: `p26-c09-comparative-repair` removes C02's singleton comparative failure and retains `COMPARATIVE_COUNT_ALL_REMAINING` with two guest deductions from a non-singleton left side. It still fails promotion because the comparison is only a one-wave opener and the remaining A3/B3 safety is solver-only.
- Comparative late-closure need: C09's R3 east-lane warning shows that simply adding a second region count to close after a comparative opener tends toward near-count giveaway. Future S2 attempts should use a later object/anchor or cross-family rule that explains A3/B3 without reducing to a two-unknown one-guest region.
- Object-gated over-closure: `p26-c11-replacement-attempt` fixes C05's target/cap problem and uses plain object copy, but the additional line/region pressure makes the opening guest layout unique. It passes correctness only as a zero-wave fixture, so it cannot replace a weak baseline case.
- Replacement repair trap: C11's lightweight anti-clone against `case-015` shows the attempted replacement removes case-015's hard singleton failures but introduces three near-count giveaway warnings and one redundant rule. Future replacement attempts should preserve opening ambiguity before adding line/region closure.
- Final synthesis blocker: `p26-c12-final-synthesis` retains `COMPARATIVE_COUNT_ALL_REMAINING` and passes its own degeneracy checks, but anti-clone reports an exact proof-trace match with C09. The added object-local rules are redundant, so the case demonstrates how easy it is to decorate a comparative opener without creating a new player experience.
- C10 repair blocker: `p26-c13-frontier-closure` shows the cost of forcing the C10 late safety gap closed by public observations and a direct no-guest region. The local-count and region-count techniques survive minimization, but R2 becomes fully observed, R1/R2/R4/R5 become redundant, and final uniqueness still fails.
- Comparative object-bridge over-closure: `p26-c14-comparative-object-bridge` repairs C12's redundant object rules by making all five rules material, but the only schema-valid way tested this round makes B1 a public mirror. That turns the comparative left side into a singleton effective scope, makes the opening guest layout unique, and erases every proof technique from the actual play experience.
- Scope-overlap derived-guest blocker: `p26-c15-overlap-chain-repair` gives the overlap rule a non-degenerate material scope and preserves opening ambiguity, but a guest derived by `REGION_COUNT_ALL_REMAINING` does not become a usable known overlap guest before the reasoner raises explanation gaps for B1/A2. Without a proof-side bridge from derived guests into later overlap summaries, scope-overlap repairs either fail to fire or fall back to C08-style opening-observation giveaways.
