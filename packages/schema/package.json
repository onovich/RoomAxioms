{
  "name": "@room-axioms/schema",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@room-axioms/domain": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.1.0"
  }
}
